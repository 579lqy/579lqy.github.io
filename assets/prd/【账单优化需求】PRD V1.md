# 「我的账单 · 月账单概览」产品需求文档（PRD）
> 隐私说明：为保护商业隐私，本文档以"**平台**"代指产品主体、"**租户**"代指客户方；原型中的示例租户名（如测试账号）已在本文档中统一脱敏为"示例租户 / 账户 A/B…"，不出现任何公司实名与品牌标识。

---
## 1. 文档概览与修订记录

| 项目 | 内容 |
|------|------|
| 文档名称 | 我的账单 · 月账单概览 需求文档 |
| 版本号 | V1.0.0 |
| 文档状态 | 已评审定稿（对应原型 V1） |
| 创建日期 | 2026-07 |
| 作者 | 产品经理-李千颖|
| 关联原型 | 【账单优化需求】原型图 V1 |
| 关联系统 | 计费系统、账户中心、账本系统、还款/支付系统、消息中心 |

### 修订记录
| 版本 | 日期 | 修改人 | 变更类型 | 修改内容 |
|------|------|--------|----------|----------|
| V1.0 | 2026-07-15 | 产品经理 | 新增 | 重排信息架构（空间切换 → 月账单概览 → 消费明细 → 账单详情/还款详情 → 导出），并定义核心业务规则（出账时间、状态机、5h 延迟、12 月窗口）与数据模型。 |
---

## 2. 需求背景与目标

### 2.1 业务背景
租户在平台产生的消费分散于多个产品/资源包（智能体服务、视频生成、各厂商 Token 量包、按量使用等），且企业客户存在**多成员账号**结构。原账单仅提供粗放汇总，租户面临三大痛点：
1. **看不懂**：无法按产品/账号归因成本，对账困难。
2. **看不全**：缺乏账本级（体验金/现金）与出账流水级下钻，争议时无从举证。
3. **对不清**：还款、代金券抵扣、账户余额、现金支付的构成不透明，财务核算受阻。

### 2.2 产品目标（SMART）
- 租户可自助查询并导出**近 12 个月**账单，线上化率 100%。
- 企业管理员对多账号账单的可见性与状态判定准确率 100%。

### 2.3 用户角色矩阵
| 角色 | 核心诉求 | 典型痛点 |
|------|----------|----------|
| 个人租户 | 看清本月花了多少、还欠多少 | 费用构成不透明 |
| 企业管理员 | 按成员账号查看/汇总账单 | 多账号散落、状态不一 |
| 企业财务 | 导出对账、查看还款与账本构成 | 缺明细、难审计 |
| 平台运营 | 保障出账按时、状态准确 | 出账延迟引发客诉 |

---

## 3. 信息架构（来自原型）

```
我的账单
├── 顶部空间切换：个人空间 | 企业空间
└── 月账单概览（主页面）
    ├── 公告区（3 条说明）
    ├── 筛选区：账单月份（近12月） + [企业空间] 账户筛选
    ├── 汇总卡
    │   ├── 周期标题 + 周期范围 + 总状态标签（悬停说明）
    │   ├── 应付金额 / 已还款金额（→还款详情）/ 待还款金额（=应付−已还）
    │   └── [导出账单] 按钮
    ├── 消费明细（按产品）
    │   ├── 表格：账单编号/账户标识(企)/产品名称/计费方式/账单月份/
    │   │        目录总价/优惠金额/优惠后金额/支付状态/操作
    │   ├── 行展开 → 账本明细（账单编号/账单月/账本类型/账户类型/实付金额）
    │   └── 操作"详情" → 账单详情页
    ├── 账单详情页（下钻）
    │   ├── 头部：账单编号/服务名称/账户标识(企)/账单时间/支付状态
    │   └── 出账流水表：出账流水号/出账时间/服务名称/账单金额/实付/待付
    └── 还款详情页（从"已还款金额"进入）
        ├── 账单月份 + 已还款金额汇总
        └── 还款明细：资源购买账号/应付/代金券抵扣/账户余额支付/现金支付/还款账户名称
```

导出弹窗（全局）：导出范围（企业筛选态可选"全部账号"）、账单月份（区间，必填）、导出类型（月账单 PDF / 产品消费汇总 CSV，必填）、商品筛选（默认全部 / 包含所选 / 排除所选）。

---

## 4. 功能清单（按优先级）

| 优先级 | 功能点 | 说明 |
|--------|--------|------|
| **P0** | 空间切换 | 个人空间 / 企业空间切换，数据隔离与口径不同 |
| **P0** | 账单月份筛选 | 日历控件，限定近 12 个月可查 |
| **P0** | 汇总卡（三金额 + 总状态） | 应付/已还/待还，含状态悬停说明 |
| **P0** | 消费明细（按产品） | 列表含计费方式、各金额列、支付状态 |
| **P0** | 行展开账本 | 查看该账单的账本构成（体验金/现金） |
| **P0** | 账单详情下钻 | 出账流水级明细 |
| **P0** | 还款详情 | 还款构成（代金券/余额/现金） |
| **P0** | 账单导出 | PDF 月账单 + CSV 产品汇总，支持月份区间与商品筛选 |
| **P1** | 企业多账号筛选 | 账户搜索 + 单选，支持"全部账户" |
| **P1** | 企业总状态综合判定 | 多成员账号状态下，管理员视图综合判定 |
| **P1** | 分页与空态 | 各表格分页（每页 3 条示例），空数据友好提示 |
| **P2** | 导出范围切换 | 企业筛选态可"改为导出全部账号" |

---

## 5. 用户故事

| 编号 | 角色 | 用户故事 | 价值 |
|------|------|----------|------|
| US-1 | 个人租户 | 作为个人租户，我希望在"月账单概览"一眼看到本月应付、已还、待还，以便掌握欠款 | 成本透明 |
| US-2 | 企业管理员 | 作为企业管理员，我希望按成员账号筛选并查看账单，以便分账管理 | 多账号可管 |
| US-3 | 企业财务 | 作为企业财务，我希望展开账本看到体验金/现金构成、并查看还款明细，以便审计核算 | 可审计 |
| US-4 | 开发者 | 作为开发者，我希望点开产品行查看账本与出账流水，以便定位某一笔费用来源 | 可举证 |
| US-5 | 任意租户 | 作为租户，我希望导出 PDF 月账单与 CSV 汇总，以便内部对账留档 | 自助对账 |

---

## 6. 业务规则与状态机（关键，来自原型逻辑）

### 6.1 出账时间规则
- **按月出账**：当月账单在**次月 2 日 12:00** 完成出账。
- 当月 12:00 前查询到的数据视为"未出账"，仅供参考、不可作为对账依据（公告第 1 条）。

### 6.2 数据延迟
- 月账单概览数据相对实际消耗**延迟 5 小时**更新（公告第 2 条）。

### 6.3 查询/导出窗口
- 仅支持查询与导出**近 12 个月**账单（公告第 3 条 + 日历控件 `MIN_IDX = CUR_IDX − 11`）。
- 日历控件：超出窗口的月份置灰不可选。

### 6.4 账单总状态机
| 状态 | 触发条件 |
|------|----------|
| **出账中** | 账单月份为当前月（尚未到次月 2 日 12:00 出账） |
| **未结清** | 已出账且 `待还款金额 > 0` |
| **已结清** | 所有欠款已还清（`待还款金额 = 0`） |

- **企业多账号综合判定**：管理员查看"全部账户"时，总状态依据所有成员账号账单状态综合判定（如有任一未结清则整体趋向未结清口径，具体以结算口径为准）。
- **状态更新时间**：
  - 历史月份：还款成功后约 **5 小时**内自动更新为"已结清"。
  - 当前月份：当月"未结清"账单还款后，系统于**次月 2 日 12:00** 统一结算并更新为"已结清"。

### 6.5 金额勾稽关系
- `优惠后金额 = 目录总价 − 优惠金额`
- `待还款金额 = 应付金额（优惠后合计） − 已还款金额`
- 账本级：`实付金额` 按账本类型（体验金 / 现金）拆分，合计 = 该账单已支付额。
- 还款级：`应付金额 = 代金券抵扣 + 账户余额支付 + 现金支付`。

### 6.6 计费方式枚举（来自数据）
包年包月、按量计费、Token 资源包、Token Plane、Token 按量使用。

---

## 7. 详细功能说明

### 7.1 月账单概览（主页面）
- **前置条件**：租户已登录，且拥有该空间/账号的账单查看权限。
- **公告区**：固定展示 3 条说明（出账时间、5 小时延迟、12 月窗口），不可关闭但可收起。
- **筛选区**：
  - 账单月份：点击打开日历，仅近 12 月可选。
  - 账户（仅企业空间）：点击打开展开下拉，支持名称/标识搜索、单选、"全部账户"。
- **汇总卡**：展示周期标题（"X 月概览"）、账单周期范围、`总状态标签`（悬停展示状态说明与更新时间）、三金额、导出按钮。
- **后置条件**：筛选/切换后表格与汇总联动刷新。

### 7.2 消费明细（按产品）
- **列**：展开箭头、账单编号、账户标识（仅企业且未指定单账号时显示）、产品名称、计费方式、账单月份、目录总价、优惠金额、优惠后金额、支付状态、操作（详情）。
- **行展开（账本）**：点击箭头展开嵌套表，列：账单编号、账单月（YYYYMM）、账本类型、账户类型、实付金额。
- **详情操作**：跳转账单详情页。

### 7.3 账单详情页（下钻）
- **头部**：账单编号、服务名称、账户标识（企业才显示）、账单时间（该月 1 日–月末）、支付状态。
- **出账流水表**：出账流水号、出账时间、服务名称、账单金额、实付金额、待支付金额。
- 顶部面包屑"返回账单列表"。

### 7.4 还款详情页
- 入口：汇总卡"已还款金额 → 查看详情"。
- 头部：账单月份、已还款金额（汇总）。
- 还款明细表：资源购买账号、应付金额、代金券抵扣金额、账户余额支付、现金支付金额、还款账户名称。
- 顶部面包屑"返回月账单概览"。

### 7.5 导出账单（弹窗）
- **导出范围**（仅企业且已筛选单账号时）：默认跟随当前筛选；可勾选"改为导出全部账号"。
- **账单月份**：必填，支持起止月份区间。
- **导出类型**：必填，可多选：月账单 PDF、产品消费汇总 CSV。
- **商品筛选**：下拉选产品 + 包含/排除所选（默认包含全部）。
- 确认后后台生成文件并通知下载。

---

## 8. 交互流程

**主流程（查看月账单）**
1. 租户进入"我的账单"，默认个人空间 / 当前月。
2. 查看公告、汇总卡三金额与总状态。
3. 按需切换账单月份（日历）/ 切换企业空间并筛选账户。
4. 浏览消费明细（按产品），必要时展开行查看账本。
5. 点击"详情"下钻到出账流水；或点击"已还款金额"查看还款构成。
6. 点击"导出账单"，在弹窗配置范围/月份/类型/商品，确认导出。

**异常流程**
- 当前月未出账：金额与状态以"出账中"呈现，并提示"数据仅供参考"。
- 筛选无数据：表格显示"暂无内容"空态。
- 导出生成中：弹窗关闭后，站内信/消息通知文件就绪。

---

## 9. 数据模型（字段语义，源自原型数据结构）

**消费明细行（按产品）**
| 字段 | 含义 | 示例 |
|------|------|------|
| billNo | 账单编号 | 100000000000041001 |
| product | 产品名称 | 智能体服务 / Doubao-Seed-2.0-pro-token… |
| sku | 规格 | 智能体服务-标准版-月度 / Token量包 / 视频生成-次卡 |
| feeType | 计费方式 | 包年包月 / 按量计费 / Token资源包 / Token Plane / Token按量使用 |
| month | 账单月份 | 2026-07 |
| list | 目录总价 | 数值（元） |
| discount | 优惠金额 | 数值（元） |
| afterDiscount | 优惠后金额 | 数值（元） |
| paidAmt | 已支付金额 | 数值（元） |
| dueAmt | 待支付金额 | 数值（元） |
| payStatus | 支付状态 | 已销账 / 待还款 |
| org | 账户标识 | 个人空间 / 企业账号标识 |

**账本（行展开）**
| 字段 | 含义 |
|------|------|
| ledgerType | 账本类型：体验金 / 现金 |
| acctType | 账户类型：支付账户(个人/企业默认) |
| paid | 该账本实付金额 |

**账单详情（出账流水）**
| 字段 | 含义 |
|------|------|
| id | 出账流水号 |
| time | 出账时间 |
| amt | 账单金额 |
| paid | 实付金额 |
| due | 待支付金额 |

**还款明细**
| 字段 | 含义 |
|------|------|
| account | 资源购买账号 |
| payable | 应付金额 |
| coupon | 代金券抵扣金额 |
| card | 账户余额支付 |
| cash | 现金支付金额 |
| payer | 还款账户名称 |

**账户（企业筛选）**
| 字段 | 含义 |
|------|------|
| acctId | 账户标识 |
| acctName | 账户名称（文档内已脱敏为"示例租户"） |
| customerName | 客户名称 |
| billCount | 账单数 |
| billTotal | 账单合计 |
| billStatus | 账单状态 |

---

## 10. 边界场景

| 场景 | 处理策略 |
|------|----------|
| 当前月（出账中）查询 | 金额/状态以"出账中"呈现，公告提示"仅供参考、不可对账" |
| 跨月出账临界点（次月 2 日 12:00 前后） | 12:00 前按"出账中"，之后按实际状态；避免状态抖动，以结算任务完成时刻为准 |
| 企业多账号混合状态 | 总状态综合判定；单个成员未结清时整体展示"未结清"口径 |
| 历史月份还款后状态未即时变"已结清" | 允许最长 5 小时延迟窗口，期内展示旧状态并提示"更新中" |
| 查询月份超出近 12 月 | 日历控件置灰禁选，接口返回空并提示超范围 |
| 账本/流水为空 | 行展开或详情页展示"暂无内容"空态，不下钻报错 |
| 导出数据量极大 | 后台异步生成，完成后通知；CSV 按产品聚合，PDF 按账期生成 |
| 企业单账号筛选态导出 | 默认跟随筛选；用户可勾选"改为导出全部账号" |
| 优惠后金额出现小数（如 31.1885） | 展示保留两位小数（¥31.19），勾稽对账以系统精度为准 |
| 无权限查看某成员账号 | 企业空间仅展示当前管理员有权限的账号，越权请求返回空 |

---

## 11. 验收标准

1. **出账准时性**：每月 2 日 12:00 前完成上月账单出账，出账后状态由"出账中"正确切换。
2. **状态准确性**：历史月份还款后 5 小时内状态更新为"已结清"；当月还款于次月 2 日 12:00 统一结算更新；准确率 ≥ 99.9%。
3. **金额勾稽**：`待还款 = 应付 − 已还`、`优惠后 = 目录 − 优惠`、账本实付合计 = 已支付、还款四项合计 = 应付，四组等式全局成立，误差 0。
4. **查询窗口**：日历仅放开近 12 个月，超范围月份不可选，接口与 UI 一致。
5. **企业多账号**：管理员在"全部账户"视图下，总状态与各成员状态逻辑一致；单账号筛选后表格仅显示该账号数据。
6. **下钻完整**：消费明细行可展开账本、可进入账单详情（出账流水）、可进入还款详情，三者数据同源一致。
7. **导出正确**：PDF 月账单与 CSV 汇总字段完整、金额精度至分，月份区间与商品筛选生效；企业筛选态"导出全部账号"开关生效。
8. **性能**：概览页首屏 P95 ≤ 2s，明细查询 ≤ 1s；每页 3 条分页在示例数据下无错位。
9. **空态与权限**：无数据展示"暂无内容"；越权账号返回空且不泄漏他人数据。

---

## 12. 非功能需求
- **性能**：见验收标准第 8 条。
- **安全与隐私**：账单含企业敏感财务信息，传输加密（TLS1.2+），按租户/账号鉴权隔离；审计日志记录查看与导出行为；文档与系统中不留存客户实名以外的冗余个人信息（示例租户名已脱敏）。
- **可靠**：出账任务具备重试与对账兜底，避免漏出账/重复出账。
- **兼容**：Web 控制台兼容主流浏览器最新两大版本；响应式适配。
- **可观测**：关键操作（筛选、下钻、导出）埋点，支持异常告警。

## 13. 数据埋点（摘要）
| 位置 | 事件 | 关键字段 |
|------|------|----------|
| 概览页 | bill_overview_view | space, billMonth, orgFilter |
| 消费明细 | bill_product_click | billNo, product, feeType |
| 账本展开 | bill_ledger_expand | billNo |
| 账单详情 | bill_detail_view | billNo |
| 还款详情 | repay_detail_view | billMonth |
| 导出 | bill_export | type(pdf/csv), monthRange, scope |

## 14. 风险与依赖
| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 出账任务延迟超 12:00 | 低 | 高 | 提前调度 + 监控告警 + 手动补出账 |
| 5 小时延迟窗口内状态不一致引发客诉 | 中 | 中 | 明确公告与悬停说明，状态旁标注"更新中" |
| 企业多账号权限越界 | 低 | 高 | 服务端强鉴权，按账号过滤 |
| 导出大数据量阻塞 | 中 | 中 | 异步生成 + 消息通知 |

---
## 附录 A 产品消费汇总样例（CSV 导出）
**用途说明**：本样例对应导出弹窗中「导出类型 = 产品消费汇总 CSV」的产物（见第 7.5 节）。数据以**产品**为最小聚合维度，字段与第 9 章「消费明细行」数据模型对齐；金额单位人民币（元），精度至分。

## 附录 B 月账单导出样例（PDF 月账单）
**用途说明**：本样例对应导出弹窗中「导出类型 = 月账单 PDF」的产物（见第 7.5 节）。呈现「月账单概览」主页面的静态快照，含汇总卡三金额、总状态与消费明细，适用于租户内部留档与财务对账。

*文档结束 / V1.0.0 定稿于 2026-08-12（对应原型 V1）*
