# 发票开具功能 · 概要设计

> 基于原型 `发票需求优化V2.html` ，字段名/函数名与原型代码保持一致。
---

## 1. 功能描述

### 1.1 功能定位
用户（ToC 个人 / ToB 企业管理员）在同一入口下，选择一批可开票的消费记录，一次提交生成发票申请。核心解决两个问题：
- 按量付费一个月产生几十条账单，不能逐条勾选 → 提供"按月周期"汇总开票
- 企业下有主账户和子账户，管理员需要跨账户统一开票，还要能拆分 → 提供"账户范围 + 合并/拆分"

### 1.2 功能清单

| 模块 | 子功能 | 适用空间 |
|---|---|---|
| 空间切换 | 个人空间 / 企业空间 tab 切换 | 全部 |
| 账户范围选择 | 搜索、全选/清空、多选账户 | 仅企业空间 |
| 发票拆分模式 | 合并为一张 / 按账户分别开票 | 仅企业空间 |
| 开票方式切换 | 按订单开票 / 按月周期开票 | 全部 |
| 周期选择器 | 当前月锁定、近3月快捷、更早月份下拉 | 仅"按周期"模式 |
| 周期汇总 | 可开票金额、订单数/可开票笔数、查看计算明细 | 仅"按周期"模式 |
| 计算明细弹窗 | 公式三项拆解 + 说明气泡 + 下钻 + 按账户拆分明细 | 全部（按账户拆分仅企业） |
| 订单筛选与列表 | 关键字/状态/日期区间筛选，批量全选/清空 | 全部 |
| 订单详情页 | 账单级明细分页展示 | 全部 |
| 公式下钻页 | 已开票 / 不可开票 账单明细跨订单汇总展示 | 全部 |
| 超限提示 | 已选金额超专票核定限额时警示 | 仅企业+合并模式 |
| 底部汇总与提交 | 已选账户数/订单数/金额，提交开票申请 | 全部 |

### 1.3 功能边界
不包含：已开具列表、发票信息（抬头）管理、真实开票网关对接、发票文件生成、发票冲红。
---

## 2. 界面设计

### 2.1 页面整体结构（自上而下）

```
顶部栏：空间切换 tab（个人空间 / 企业空间）+ 用户信息
页面标题（随空间变化："发票管理" / "企业发票管理"）
二级 tab：开具发票（当前） / 已开具列表 / 发票信息（抬头）
规则提示条（notice）：4 条开票规则说明
──────────────────────────
[企业空间独有] 账户范围卡片
  - 拆分模式单选：合并为一张发票 / 按账户分别开票
  - 账户下拉选择器（默认收起）
──────────────────────────
开票方式卡片
  - 两张并列卡片：按订单开票 / 按月周期开票（含各自适用场景说明文案）
──────────────────────────
[按周期模式独有] 开票周期卡片
  - 当前月 chip（禁用态，灰色，文案含"本月未出账"）
  - 近3月 chip（可点击）
  - 更早月份下拉（第4~12月）
[按周期模式独有] 本周期汇总卡片
  - 大字金额 + "共X笔订单，X笔可开票"
  - "查看计算明细 ›" 链接
  - 超限警示条（条件展示）
──────────────────────────
订单列表卡片
  - 筛选行（按模式切换表单：周期模式=关键字+状态；订单模式=关键字+开始日期+结束日期+状态）
  - 结果提示 + 批量操作（全选筛选结果 / 清空本次选择）
  - 订单表格：复选框 | 订单信息 | [所属账户]* | [账期]** | 计费方式 | 订单总额 | 可开票金额 | 状态标签 | 详情
    * 仅企业空间展示　** 仅周期模式展示
──────────────────────────
底部悬浮汇总栏
  - [企业]已选账户数 / 已选订单数 / 已选开票金额 / 拆分提示文案
  - 提交按钮（"申请开票" 或 "提交拆分开票申请"）
```

### 2.2 弹窗与二级页面

**计算明细弹窗**（居中模态）
```
标题：可开票金额
可开票金额（大字橙色）
  = 总计消费可开票金额（? 说明气泡）
  − 历史已开票金额（? 说明气泡，"查看明细"链接）
  − 本月不可开票金额（? 说明气泡，"查看明细"链接）
[企业空间] 按账户拆分明细（可折叠展开）
关闭按钮
```

**订单详情页**（替换主内容区，非弹窗，带返回按钮）
```
‹ 返回　订单详情 · {订单号}
账单明细表格（账单编号/账单月/账本类型/账户类型/实付金额/开票状态）
分页条：上一页 | 第X/Y页，共Z条 | 下一页
（一次性订单：显示"该订单为一次性结算，无逐笔账单明细"）
```

**公式下钻页**（替换主内容区，带返回按钮）
```
‹ 返回　历史已开票金额 · 关联账单 / 本月不可开票金额 · 关联账单
跨订单汇总账单表（账单编号/账单月/账本类型/账户类型/实付金额/开票状态/订单号）
```

### 2.3 状态标签色系（视觉规范）

| 状态 | 颜色 | 文案 |
|---|---|---|
| ok | 绿 | 可开票 |
| partial | 橙 | 部分可开票 |
| invoiced | 蓝 | 已开票 |
| no | 红 | 不可开票 |
| pending（订单级） | 灰 | 未出账 |

主题色：`--blue: #1677d6`（操作/强调），`--orange: #ff7a1a`（金额强调）。

---

## 3. 处理流程

### 3.1 页面初始化流程

```
加载页面
 → 初始化账户列表（企业：默认全选全部账户）
 → 初始化周期选择器（计算近3月 chip + 4~12月下拉，默认选中最近已完结月）
 → 计算当前范围（空间 × 账户范围）内的订单可开票金额、状态
 → 渲染周期汇总 / 订单列表（默认全选 invoiceable>0 的订单）
 → 渲染底部汇总栏
```

### 3.2 订单状态计算流程（核心处理逻辑）

```
输入：订单 { amount, invoicedAmt, excludedAmt, recurring, period }
Step1: invoiceable = MAX(0, amount − invoicedAmt − excludedAmt)
Step2: 判定状态
  IF recurring=true AND period=当前自然月 → status=pending（未出账，锁定不可选）
  ELSE IF invoiceable=amount → status=ok
  ELSE IF invoiceable>0 → status=partial
  ELSE IF invoicedAmt>0 → status=invoiced
  ELSE → status=no
Step3: 渲染对应标签与复选框可用性（invoiceable=0 时复选框禁用）
```

### 3.3 筛选与选择联动流程

```
用户操作（切空间 / 切模式 / 切周期 / 变更账户范围）
 → 清空 selected（已选订单集合归零，避免跨维度脏数据）
 → 重新计算当前范围内订单
 → 重新渲染列表（invoiceable>0 的订单默认自动勾选）
 → 重新渲染底部汇总栏
```

```
用户操作（筛选表单查询）
 → 在当前范围订单基础上按 关键字/状态/日期 过滤
 → 更新 lastList（供"全选筛选结果/清空本次选择"使用）
 → 渲染结果提示文案 + 列表
```

### 3.4 提交开票流程

```
用户点击"申请开票" / "提交拆分开票申请"
 → 汇总 selected 中的订单：已选订单数、已选金额、涉及账户数
 → IF 企业+合并模式 AND 已选金额>核定限额 → 已在页面持续展示警示，不阻断提交
 → IF 企业+拆分模式 → 按账户分组，逐账户生成一张发票申请
 → 提交（原型为 mock alert，实际应调用开票申请接口）
 → 成功后应清空已选、提示结果（原型未实现，需在详设阶段补齐）
```

### 3.5 计算明细弹窗数据流程

```
点击"查看计算明细"
 → 取当前范围（空间×账户×周期）内全部订单
 → 汇总：总额 / 已开票 / 不可开票 / 可开票 四个数字
 → [企业] 按已选账户分别求和可开票金额，过滤掉为0的账户
 → 渲染弹窗
```

---

## 4. 交互描述

| # | 交互点 | 触发方式 | 反馈 |
|---|---|---|---|
| 1 | 空间切换 | 点击顶部 tab | 立即切换页面标题、显示/隐藏企业专属卡片，已选订单清空 |
| 2 | 拆分模式切换 | 点击"合并/按账户"文字 | 单选态切换（radio 高亮），底部提交按钮文案联动 |
| 3 | 账户下拉展开/收起 | 点击触发器 / 点击页面空白处 | 展开显示搜索框+列表；点击外部区域自动收起 |
| 4 | 账户搜索 | 输入框实时输入 | 列表实时过滤（无节流/防抖，原型为同步过滤） |
| 5 | 账户勾选 | 点击整行 | 复选框切换态，触发器文案更新（"XX等N个账户" 规则见2.2） |
| 6 | 账户全选/清空 | 点击"全选"/"清空"按钮 | 批量勾选或取消全部账户 |
| 7 | 开票方式切换 | 点击卡片 | 卡片高亮态切换，下方筛选表单和列表列结构联动切换 |
| 8 | 周期 chip 点击 | 点击 chip / 下拉选择"更早月份" | 高亮切换到目标周期，已选订单清空，列表和汇总重新渲染 |
| 9 | 当前月 chip | 不可点击（disabled 样式） | 无响应，仅作说明用途 |
| 10 | 查看计算明细 | 点击文字链接 | 打开居中弹窗，点击遮罩层或"关闭"按钮/×关闭 |
| 11 | 说明气泡（?图标） | 点击 | 展开黑底提示条，同一时刻仅一个气泡展开，再点其他?自动收起当前 |
| 12 | 查看明细（历史已开票/不可开票） | 点击文字链接 | 关闭弹窗，跳转到下钻明细页（非新开弹窗，是页面替换） |
| 13 | 按账户拆分明细 | 点击折叠标题 | 展开/收起子列表，箭头图标随之翻转 |
| 14 | 筛选查询/重置 | 点击按钮 | 查询：按当前表单条件重新过滤列表；重置：表单恢复默认值并重新查询 |
| 15 | 全选筛选结果 | 点击 | 对当前筛选结果中 invoiceable>0 的订单全部勾选（不影响列表外的其他订单选择态） |
| 16 | 清空本次选择 | 点击 | 仅清空当前筛选结果范围内的已选订单 |
| 17 | 订单行复选框 | 点击 | invoiceable>0 时可切换选中态；=0 时禁用态，点击无响应 |
| 18 | 订单"详情" | 点击 | 页面整体替换为订单详情页（非弹窗），返回按钮回到列表并保留原选择态 |
| 19 | 详情页分页 | 点击上一页/下一页 | 页码±1并重渲染表格；首页禁用"上一页"，末页禁用"下一页" |
| 20 | 提交开票 | 点击底部按钮 | 触发提交流程（原型为 alert 提示，正式版需替换为真实提交+loading+结果反馈） |
| 21 | 超限警示 | 已选金额超限时自动出现，无需用户操作 | 橙色提示条，不阻断提交按钮可点性 |

---

## 5. 测试用例

### 5.1 状态计算与金额

| 用例ID | 场景 | 前置条件 | 操作 | 预期结果 |
|---|---|---|---|---|
| TC-01 | 持续计费订单落在当前自然月 | recurring=true, period=当前月 | 打开订单列表 | 状态="未出账"（灰），复选框禁用，无论金额是否为0 |
| TC-02 | 一次性订单落在当前自然月 | recurring=false, period=当前月, invoiceable>0 | 打开订单列表 | 状态非"未出账"，按金额正常判定 ok/partial，复选框可勾选（验证"当月锁定"规则不误伤一次性订单） |
| TC-03 | 订单全额可开票 | invoiceable = amount | 查看状态标签 | 显示"可开票"（绿） |
| TC-04 | 订单部分可开票 | 0 < invoiceable < amount | 查看状态标签 | 显示"部分可开票"（橙） |
| TC-05 | 订单已全部开票 | invoiceable=0, invoicedAmt>0 | 查看状态标签 | 显示"已开票"（蓝） |
| TC-06 | 订单不可开票（如全额体验金） | invoiceable=0, invoicedAmt=0 | 查看状态标签 | 显示"不可开票"（红） |
| TC-07 | 可开票金额计算 | amount=560, invoicedAmt=20, excludedAmt=60 | 查看订单行"可开票金额" | 480.00（560-20-60），且不出现负数 |
| TC-08 | 可开票金额下限保护 | invoicedAmt+excludedAmt > amount（异常脏数据） | 查看计算结果 | 显示 0.00，不出现负数 |
| TC-09 | 金额截断（分以下舍去） | 存在带3位以上小数的原始金额（后端场景） | 查看展示与汇总 | 应向下截断到分，而非四舍五入 |

### 5.2 周期选择器

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-10 | 当前月锁定 | 打开周期选择器 | 当前月 chip 为禁用态，文案含"本月未出账"，点击无响应 |
| TC-11 | 近3月快捷入口 | 打开周期选择器 | 展示最近3个已完结自然月的可点击 chip |
| TC-12 | 第4~12月收进下拉 | 打开"更早月份"下拉 | 下拉选项包含第4~12个已完结月，选中后同步高亮，且不重复出现在常驻chip中 |
| TC-13 | 默认选中周期 | 首次进入页面 | 默认选中最近一个已完结自然月，非当前月 |
| TC-14 | 切换周期清空选择 | 已勾选若干订单后切换周期 | 已选订单清空，底部汇总归零 |

### 5.3 账户范围（企业空间）

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-15 | 默认全选账户 | 首次进入企业空间 | 全部账户默认勾选，触发器文案显示"全部账户（N个）" |
| TC-16 | 账户搜索 | 输入不存在的关键字 | 列表显示"没有匹配的账户" |
| TC-17 | 单选/取消单个账户 | 点击某账户行 | 该账户勾选态切换，触发器文案实时更新 |
| TC-18 | 全选/清空按钮 | 点击"全选"/"清空" | 全部账户批量勾选/取消，联动触发订单列表重新过滤 |
| TC-19 | 变更账户范围清空选择 | 已勾选订单后变更账户勾选状态 | 已选订单清空，列表按新范围重新渲染 |
| TC-20 | 个人空间不展示账户范围卡片 | 切换到个人空间 | "账户范围"卡片、"按账户拆分"选项、列表"所属账户"列均隐藏 |

### 5.4 拆分开票与限额

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-21 | 合并模式超限提示 | 企业+合并模式，已选金额>核定限额（示例¥5000） | 查看页面 | 出现橙色警示条，显示已选金额与限额数值，建议改拆分 |
| TC-22 | 合并模式未超限 | 已选金额≤限额 | 查看页面 | 无警示条 |
| TC-23 | 切换拆分模式隐藏警示 | 在TC-21基础上切换为"按账户拆分" | 查看页面 | 警示条隐藏（⚠需人工确认：拆分后单账户是否仍超限未被校验，见风险项） |
| TC-24 | 拆分模式提交文案 | 拆分模式下已选订单覆盖2个账户 | 查看底部 | 提示"将按账户分别生成 2 张发票申请"，按钮文案"提交拆分开票申请" |
| TC-25 | 拆分模式覆盖账户数为0 | 拆分模式但未勾选任何订单 | 查看底部 | 不显示拆分提示文案，按钮回退为"申请开票" |

### 5.5 筛选与批量操作

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-26 | 周期模式关键字筛选 | 输入订单号片段查询 | 列表仅展示匹配订单，结果提示笔数正确 |
| TC-27 | 周期模式状态筛选 | 选择"可开票" | 仅展示 status=ok 的订单 |
| TC-28 | 订单模式日期区间筛选 | 设置开始/结束日期 | 仅展示 date 落在区间内的订单，含边界值（等于开始/结束日期均应包含） |
| TC-29 | 订单模式状态含"未出账" | 选择"未出账" | 展示 status=pending 的订单（订单模式筛选项比周期模式多一个"未出账"选项，需核对） |
| TC-30 | 重置筛选 | 点击"重置" | 表单恢复默认值（订单模式恢复默认日期区间），重新查询 |
| TC-31 | 全选筛选结果 | 筛选出5笔，其中1笔 invoiceable=0 | 点击"全选筛选结果" | 4笔可开票订单被勾选，1笔禁用订单不受影响 |
| TC-32 | 清空本次选择 | 已勾选若干订单 | 点击"清空本次选择" | 当前筛选结果范围内的订单全部取消勾选 |
| TC-33 | 列表空状态 | 筛选条件下无匹配订单 | 查看列表区域 | 显示"没有符合筛选条件的订单，试试调整筛选项或账户范围" |

### 5.6 计算明细与下钻

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-34 | 打开计算明细弹窗 | 点击"查看计算明细" | 弹窗展示四个金额，且 可开票=总额-已开票-不可开票 恒等式成立 |
| TC-35 | 说明气泡 | 点击任一"?"图标 | 展开对应说明文字；再点另一个"?"，前一个自动收起 |
| TC-36 | 下钻"历史已开票"明细 | 点击该项"查看明细" | 弹窗关闭，跳转到下钻页，展示相关账单行，含订单号列 |
| TC-37 | 下钻"本月不可开票"明细 | 点击该项"查看明细" | 同上，展示体验金/未结算相关账单行 |
| TC-38 | 按账户拆分明细（企业） | 展开该折叠区 | 展示已选账户各自可开票金额，金额为0的账户不展示；全为0时显示提示文案 |
| TC-39 | 账单明细 tag 语义 | 后端返回账单 tag='pending' | 界面应展示"已开票"（真实货币），而非误读为"未出账" | 
| TC-40 | 关闭弹窗 | 点击遮罩层/×/关闭按钮 | 弹窗关闭，同时收起所有展开的说明气泡与按账户拆分明细 |

### 5.7 订单详情与分页

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-41 | 一次性订单详情 | 点击一次性订单"详情" | 显示"该订单为一次性结算，无逐笔账单明细"，无分页条 |
| TC-42 | 持续计费订单详情 | 点击有多条账单的订单"详情" | 展示账单表格，含分页条 |
| TC-43 | 分页首页 | 详情页默认第1页 | "上一页"按钮禁用 |
| TC-44 | 分页末页 | 翻到最后一页 | "下一页"按钮禁用 |
| TC-45 | 分页条目数超过10条 | 某订单有15条账单明细 | 每页展示10条，共2页，页码指示"第1/2页，共15条" |
| TC-46 | 返回列表 | 点击"‹返回" | 回到订单列表，之前的已选订单状态保留不变 |

### 5.8 提交流程

| 用例ID | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-47 | 未选任何订单提交 | 已选订单数=0 | 点击提交 | 应提示"请至少选择一笔订单"（原型未实现校验，需补充） |
| TC-48 | 正常提交（合并） | 已选N笔订单，合并模式 | 点击"申请开票" | 提交成功反馈，展示涉及的金额与笔数 |
| TC-49 | 正常提交（拆分） | 已选订单覆盖多账户，拆分模式 | 点击"提交拆分开票申请" | 按账户生成对应数量的发票申请 |
| TC-50 | 重复点击提交 | 网络较慢时连续点击提交按钮 | 点击两次 | 不应重复提交两次申请（原型未做防抖/禁用态，需在详设中补齐） |

---

## 附注（与开发对齐用）

- 本文档的用例 TC-23、TC-39、TC-47、TC-50 对应上一版 PRD 中列出的未闭环风险项，属于"原型未覆盖但正式开发必须补齐"的用例，测试阶段需单独标记。
- 数据结构、字段定义、状态机公式均以《【发票优化需求】PRD_V2》为准，本文档只做界面/流程/交互/用例层面的补充，不重复定义字段。
